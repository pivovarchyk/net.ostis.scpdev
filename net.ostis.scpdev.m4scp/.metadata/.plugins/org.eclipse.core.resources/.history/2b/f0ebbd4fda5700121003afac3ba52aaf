// Заголовочный файл с описанием синонимом для
// ключевых узлов языка SCP. Файл scp_keynodes.scsy
// должен быть подключен в любом m4scp-файле.
//
#include "scp_keynodes.scsy"

// Объявление программы hello_world
program(hello_world,
[[
	// Секция констант scp-программы (здесь возможен любой sc.s-текст)
	message =c= /"Hello world!!!"/;
]],
[{
	var1 // Секция переменных scp-программы
}])
	// Здесь начинается тело scp-программы
	
	// Оператор вывода содержимого sc-узла message на консоль
	printNl([1_: fixed_: message])
	printNl([1_: fixed_: message])
	
	
	// Оператор возврата из программы
	return()
end // Здесь заканчивается тело scp-программы
